---
import '@fontsource-variable/inter';
//const colors = require('tailwindcss/colors');

// Nunito
// Dosis
---

<style is:inline is:global>
  :root {
    --cachyos-font-sans: 'Inter Variable';
    --cachyos-font-serif: var(--cachyos-font-sans);
    --cachyos-font-heading: var(--cachyos-font-sans);

    --cachyos-color-primary: rgb(139, 92, 246);
    --cachyos-color-secondary: rgb(99, 102, 241);
    --cachyos-color-accent: rgb(45, 212, 191);
  }
</style>
